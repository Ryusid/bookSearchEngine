name: Build & Push Frontend Image

on:
  push:
    branches: [ main ]
    paths:
      - "web/**"
      - ".github/workflows/frontend.yml"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE=ghcr.io/$OWNER/book-frontend" >> $GITHUB_ENV
          echo "TAG=sha-${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Build & push frontend image
        uses: docker/build-push-action@v5
        with:
          context: web
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ env.TAG }}
            ${{ env.IMAGE }}:latest

      - name: Update frontend image tag
        run: |
          sed -i \
            "/name: ghcr.io\/ryusid\/book-frontend/{n;s/newTag:.*/newTag: ${TAG}/}" \
            k8s/kustomization.yml

      - name: Commit & push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/kustomization.yml
          git commit -m "chore(frontend): bump image to ${TAG}" || exit 0
          git push
